
# Simple Files Compressor (SFC) 开发日志

## 目录

- [Simple Files Compressor (SFC) 开发日志](#simple-files-compressor-sfc-开发日志)
  - [目录](#目录)
  - [项目概述](#项目概述)
  - [版本历史摘要](#版本历史摘要)
  - [核心模块实现记录](#核心模块实现记录)
    - [已实现模块](#已实现模块)
    - [预留模块（未使用）](#预留模块未使用)
  - [技术难点与解决方案](#技术难点与解决方案)
    - [1. Huffman 单字符问题](#1-huffman-单字符问题)
    - [2. 目录块边界对齐问题](#2-目录块边界对齐问题)
    - [3. 长路径支持（现已放弃）](#3-长路径支持现已放弃)
    - [4. LTO 编译问题](#4-lto-编译问题)
    - [5. 偏移量动态维护](#5-偏移量动态维护)
  - [性能指标](#性能指标)
  - [待优化项](#待优化项)
  - [开发时间线](#开发时间线)
    - [2026-04-18：接口收口、编码统一与 GUI 多语言维护](#2026-04-18接口收口编码统一与-gui-多语言维护)
    - [2026-04-17：策略模式重构 — 压缩/加密模块抽象化](#2026-04-17策略模式重构--压缩加密模块抽象化)
    - [2026-04-16-S2：QT框架性能优化与代码重构](#2026-04-16-s2qt框架性能优化与代码重构)
    - [2026-04-16-S1：GUI 界面优化与精简部署](#2026-04-16-s1gui-界面优化与精简部署)
    - [2026-04-15：GUI 开发与关键 Bug 修复](#2026-04-15gui-开发与关键-bug-修复)
    - [2026-04-15：文档更新](#2026-04-15文档更新)
    - [2026-04-12：代码重构](#2026-04-12代码重构)
    - [2026-04-01：构建系统迁移](#2026-04-01构建系统迁移)
    - [2026-03-08 ~ 2026-03-15：功能完善](#2026-03-08--2026-03-15功能完善)
    - [2026-03-02 ~ 2026-03-07：v1.0.1 Stable \& v1.1.1 Well Done](#2026-03-02--2026-03-07v101-stable--v111-well-done)
    - [2026-01-02 ~ 2026-01-06：代码优化](#2026-01-02--2026-01-06代码优化)
    - [2026-01-01：v1.0.0 Preview 发布](#2026-01-01v100-preview-发布)
    - [2025-12-30 ~ 2025-12-31：关键 Bug 修复](#2025-12-30--2025-12-31关键-bug-修复)
    - [2025-12-21 ~ 2025-12-25：文档完善](#2025-12-21--2025-12-25文档完善)
    - [2025-12-18 ~ 2025-12-19：代码规范化](#2025-12-18--2025-12-19代码规范化)
    - [2025-12-15 ~ 2025-12-17：解压模块迭代](#2025-12-15--2025-12-17解压模块迭代)
    - [2025-12-13 ~ 2025-12-14：解压功能开发](#2025-12-13--2025-12-14解压功能开发)
    - [2025-12-10 ~ 2025-12-12：HeaderIterator 迭代器开发](#2025-12-10--2025-12-12headeriterator-迭代器开发)
    - [2025-12-08 ~ 2025-12-09：AES 加密模块完成](#2025-12-08--2025-12-09aes-加密模块完成)
    - [2025-12-03 ~ 2025-12-06：HeaderWriter 功能扩展](#2025-12-03--2025-12-06headerwriter-功能扩展)
    - [2025-12-01 ~ 2025-12-02：数据传输接口](#2025-12-01--2025-12-02数据传输接口)
    - [2025-11-01 ~ 2025-11-13：HeaderReader 原型迭代与路径兼容性打底](#2025-11-01--2025-11-13headerreader-原型迭代与路径兼容性打底)
    - [2025-11-17 ~ 2025-11-21：HeaderLoader、DirectoryReader 与压缩主链路成型](#2025-11-17--2025-11-21headerloaderdirectoryreader-与压缩主链路成型)
    - [2025-11-29 ~ 2025-11-30：HeaderLoader 与 AES-CFB](#2025-11-29--2025-11-30headerloader-与-aes-cfb)
    - [2025-11-27 ~ 2025-11-28：HeaderWriter 调试](#2025-11-27--2025-11-28headerwriter-调试)
    - [2025-11-25 ~ 2025-11-26：编码树序列化](#2025-11-25--2025-11-26编码树序列化)
    - [2025-11-23 ~ 2025-11-24：HeaderWriter 与 Huffman 编码表](#2025-11-23--2025-11-24headerwriter-与-huffman-编码表)
    - [2025-10-28 ~ 2025-10-30：任务调度优化 Day5](#2025-10-28--2025-10-30任务调度优化-day5)
    - [2025-10-26：流程图设计 Day3-4](#2025-10-26流程图设计-day3-4)
    - [2025-10-23 ~ 2025-10-24：线程池与任务队列 Day2](#2025-10-23--2025-10-24线程池与任务队列-day2)
    - [2025-10-21：框架搭建 Day0](#2025-10-21框架搭建-day0)
    - [2025-10-11 ~ 2025-10-18：架构设计与规划](#2025-10-11--2025-10-18架构设计与规划)
    - [2025-10-01 ~ 2025-10-05：AES 加密模块开发](#2025-10-01--2025-10-05aes-加密模块开发)
    - [2025-09-29 ~ 2025-09-30：项目初始化](#2025-09-29--2025-09-30项目初始化)

---

## 项目概述

**SFC.exe** 是一个基于 **Huffman 编码与 AES-128 加密**实现的文件归档工具，支持对**多个文件与目录进行压缩与解压缩**。项目主要用于研究文件系统归档结构、数据压缩以及基础加密实现。

**核心特性**：
- Huffman 无损压缩，最佳压缩率约 60%
- AES-128 对称加密，SHA-256 密钥派生
- 8MB 分块处理，满载内存峰值控制在 100MB

---

## 版本历史摘要

| 版本 | 日期 | 状态 | 主要更新 |
|------|------|------|----------|
| v2.1.0 | 2026-04-16 | Strategy | 策略模式重构，4种压缩模式，接口抽象化，解压自动检测，Qt 静态链接（57MB→16MB），中文路径解压修复 |
| v2.0.0 | 2026-04-16 | GUI Release | Qt 图形界面，精简部署，Windows 兼容性优化，中文路径支持 |
| v1.1.1 | 2026-03-07 | Well Done | 封装文件 I/O，修复目录分块问题，内存稳定 60MB |
| v1.0.1 | 2026-01-01 | Stable | 修复 Huffman 编码边界问题，O3 优化 |
| v1.0.0 | 2026-01-01 | Preview | 初始版本，存在 Huffman 树构建 Bug |

---

## 核心模块实现记录

### 已实现模块

| 模块 | 文件 | 功能描述 |
|------|------|----------|
| **FileLibrary** | FileLibrary.h | 核心类型定义、常量、文件头结构 |
| **HeaderWriter** | HeaderWriter.h | 文件头写入，策略模式支持多版本 |
| **EntryProcessor** | EntryProcessor.h | BFS 目录扫描，文件结构序列化 |
| **BinaryStandardWriter** | BinaryStandardWriter.h | 二进制格式标准化写入 |
| **BinaryStandardLoader** | BinaryStandardLoader.h | 二进制目录加载与 AES 解密 |
| **EntryParser** | EntryParser.h | 解析二进制目录条目 |
| **DataLoader** | DataLoader.h | 分块读取源文件数据（8MB） |
| **DataExporter** | DataExporter.h | 写入压缩/解压数据，支持偏移量回填 |
| **Heffman** | Heffman.h | Huffman 压缩核心：频率统计、树构建、编码解码 |
| **Aes** | My_Aes.h | AES-128 加密：密钥扩展、加解密、预计算表优化 |
| **CompressionLoop** | MainLoop.h | 压缩流程主循环 |
| **DecompressionLoop** | MainLoop.h | 解压流程主循环 |

### 预留模块（未使用）

| 模块 | 路径 | 描述 |
|------|------|------|
| **ThreadPool** | ThreadPool/ | 线程池管理，命名线程，任务队列 |
| **Scheduler** | Schedule/ | 调度器，策略模式，Worker 接口 |
| **DataBlocksManage** | DataBlocks/ | 数据块管理，传输接口 |
| **Hefftype** | CompressionModules/hefftype/ | Huffman 数据结构：哈希表、最小堆、树 |

---

## 技术难点与解决方案

### 1. Huffman 单字符问题

**问题**：数据块只包含一种字符时，最小堆无法正确合并。

**解决**：检测单字符情况，直接编码为 "0"。

### 2. 目录块边界对齐问题

**问题**：目录数据恰好 16KB 对齐时出现空文件。

**解决**：补充迭代流程，确保边界正确处理。

### 3. 长路径支持（现已放弃）

**问题**：Windows 默认路径长度限制 260 字符。

**解决**：添加 `\\?\` 前缀支持长路径。

### 4. LTO 编译问题

**问题**：启用链接时优化后程序异常。

**解决**：仅使用 O3 优化，禁止 LTO。

### 5. 偏移量动态维护

**问题**：文件流缓冲导致 `tellg/tellp` 更新延迟。

**解决**：软件层动态维护 `tempOffset` 变量。

---

## 性能指标

| 指标 | 数值 |
|------|------|
| 数据块大小 | 8MB |
| 目录缓冲 | 16KB |
| 内存峰值 | 约 100MB（Qt 引入后） |
| 最佳压缩率 | 约 60% |
| 测试场景 | 13,000 个文件/目录，230GB 数据 |
| 动态部署体积 | 57 MB（exe + DLL + 插件） |
| 静态部署体积 | 16 MB（单 exe） |

---

## 待优化项

- [ ] 实现 SIMD/AES-NI 指令加速
- [ ] 支持多种压缩算法（LZ77、LZMA）— 策略模式框架已就绪，可扩展
- [x] 添加图形界面（GUI）
- [x] Qt 静态链接（单 exe 部署，16MB）
- [ ] 增加进度显示优化
- [ ] 实现多线程流水线处理
- [ ] 添加单元测试

---

## 开发时间线

### 近期维护记录（新→旧）

### 2026-04-18：接口收口、编码统一与 GUI 多语言维护

**开发内容**：
- 结合当日重构提交，继续对压缩/解压调用链做接口收口与代码风格统一，整理头文件声明、命名空间引用和模块边界。
- 统一字符编码调用标准，补充 `EncodingUtils` 相关转换能力，减少 Qt、UTF-8 与 `std::filesystem` 混用时的编码歧义。
- 对 `Y_flib` 相关引用与部分流程细节继续清理，补强目录队列、数据导出和路径处理的衔接一致性。
- GUI 侧补充简体中文翻译资源，启动时按系统语言自动选择，同时保留 `SFC_GUI_LANGUAGE` 覆盖能力；日志与技术状态文本继续保持英文。
- 多语言资源改为内嵌进程序资源，默认保持单 exe 打包；同时修正翻译资源路径，验证系统中文和强制英文切换都能生效。
- 工程清理方面，补充本地构建目录的 `.gitignore` 规则，并删除临时验证构建目录。

**参考提交摘要**：
- `interface refactor`
- `standardize code style`
- `Unified character encoding invocation standards`
- `Y_flib`

### 2026-04-17：策略模式重构 — 压缩/加密模块抽象化

**目标**：借助接口抽象化调用流程，使得可以动态切换不同压缩或加密算法模块，并通过文件头策略号自动识别。

- 新增 `CompressionMode`、`StrategyFactory`、`NullCompression`、`NullEncryption`，把压缩/加密组合从固定实现改成可切换策略。
- 主流程与 GUI 一并完成接口化：压缩页可选择模式，解压侧按文件头策略号自动识别，旧 `strategy=0` 文件继续兼容。
- 枚举设计、工厂装配、树桩格式、接口签名调整与文件清单已迁入《开发流程与设计细节》。

### Qt 6.2.4 静态链接（v2.1.0）

**结果**：
- GUI 工程增加 `USE_STATIC_QT` 构建开关，静态单包体与动态部署路径正式分离。
- 发布体积从约 57MB 收敛到约 16MB，翻译与图片资源继续保持内嵌。
- Qt 静态库构建、部署切换与工具链注意事项已迁入《开发流程与设计细节》。

### 解压中文路径 Bug 修复（v2.1.0）

- 修复了解压输出目录包含中文时的路径乱码问题，原因是 UTF-8 字符串被直接构造成 `std::filesystem::path`。
- 处理方式是先走统一编码转换，再交给 `DecompressionLoop` 的输出目录路径对象。
- 编码约定和该修复的上下文已迁入《开发流程与设计细节》。



### 2026-04-16-S2：QT框架性能优化与代码重构

**优化内容**：

- 修复文件句柄释放与异常路径资源回收问题。
- 优化缓冲区复用与 Huffman 内存释放，缓解长流程下的内存持续增长。
- 为 `MainLoop` / `CompressionWorker` 增加进度节流，避免 GUI 卡顿与信号堆积。
- 补齐停止标志与窗口关闭时的线程回收。
- 将主流程拆分为 `CompressionLoop`、`DecompressionLoop` 等模块，并修复 `DataExporter` 作用域导致的数据未写入问题。
- 构建、部署与包体压缩细节已迁入《开发流程与设计细节》。

---

### 构建与维护（2026年4月）

#### 2026-04-16-S1：GUI 界面优化与精简部署

**开发内容**：
- 重构 GUI 布局与交互，明确压缩/解压输入区、结果区和后台任务反馈路径。
- 处理 Qt 版本、Windows 兼容性、中文路径与后台线程协作等 GUI 落地问题，完成首个可发布图形版本。
- 完成从动态精简部署到后续单包体方案的过渡验证，相关构建与配置方法已迁入《开发流程与设计细节》。

##### v2.0.0 — GUI Release

- Qt 图形界面正式可用，支持拖放、进度显示、日志输出与基础参数配置。
- GUI 发布链路打通，后续维护以 `Y_Manager_GUI` 为主。
- 编写、兼容性、部署与多语言配置细节已统一迁入《开发流程与设计细节》。

---

#### 2026-04-15：GUI 开发与关键 Bug 修复

**开发内容**：
- 建立 `Y_Manager_GUI` 工程，完成 GUI 初版界面、资源接入和后台任务执行框架。
- 修复 `directoryOffset` 写入时机错误，确保目录偏移记录正确。
- 初步打通 Qt CMake 配置、线程模型与部署流程；相关编写与配置方法已整理到《开发流程与设计细节》。

---

#### 2026-04-15：文档更新

**开发内容**：
- 更新 README 文档
- 更新项目说明、开发流程文档
- 撰写开发日志
- 准备新版本发布

---

#### 2026-04-12：代码重构

**开发内容**：
- 进行代码重构优化
- 清理冗余代码
- 统一代码风格

---

#### 2026-04-01：构建系统迁移

**开发内容**：
- **从 GCC 迁移到 MSVC 编译器**
- 添加 CMake 构建支持
- 更新 IV 大小定义
- 更新 `.gitignore` 配置
- 最终目的是运行VS的性能检测

**CMake 配置**：
```cmake
cmake_minimum_required(VERSION 3.20)
project(SFC)

set(CMAKE_CXX_STANDARD 20)
set(CMAKE_CXX_FLAGS_RELEASE "-O3")

# 禁止 LTO
set(CMAKE_INTERPROCEDURAL_OPTIMIZATION OFF)

add_executable(SFC main.cpp ...)
```

---

### 稳定版本（2026年3月）

#### 2026-03-08 ~ 2026-03-15：功能完善

**开发内容**：
- 添加项目说明文档
- **处理长路径问题**（>260字符）-最后其实没能落地，由于api的狮山问题放弃
  ```cpp
  std::filesystem::path PathTransfer::transPath(const std::string& path)
  {
      // 添加 \\?\ 前缀支持长路径
      if (p.is_absolute()) {
          return std::filesystem::path(L"\\\\?\\") / p;
      }
      return p;
  }
  ```
- 优化文件存在性检查
- 修复目录队列填充逻辑
- **移除 OpenSSL 依赖**（使用 Windows CryptoAPI）

---

#### 2026-03-02 ~ 2026-03-07：v1.0.1 Stable & v1.1.1 Well Done

**开发内容**：
- 大量代码规范化工作
- 重命名类和函数以提高可读性
- 引入 `enum class FlagType : char` 枚举类型
- 完善队列接口模板类

**关键改进**：
```cpp
// 强类型枚举，避免隐式转换
enum class FlagType : char
{
    Directory = '0',
    File = '1',
    Separated = '2',
    LogicalRoot = '3',
    SymbolLink = '4'
};
```

**修复问题**：
- 目录块分块问题：大于或等于 16KB 时边界对齐问题
- （做的修复具体来说是使每个块的开头正确执行了条目的迭代，位置在compressionLoop.cpp）
- 文件已存在时的跳过问题

---

### 优化与发布（2026年1月）

#### 2026-01-02 ~ 2026-01-06：代码优化

**优化内容**：
- `constexpr const` 简化为 `constexpr`
- 将目录偏移量类型统一到 `uint64_t`，规避 32 位环境下大于 4GB 偏移定位失效的问题
- 定义 `DIRECTORY_BUFFER` 常量（16KB）
- 同期补充 README 与项目说明，为后续稳定版发布做准备

---

#### 2026-01-01：v1.0.0 Preview 发布

**发布内容**：
- 添加应用图标（`.ico`）
- 修复 Huffman 编码边界问题
- 优化命令行交互

**已知问题**：
- Huffman 树在处理单字符输入时构建不正确（后续修复）

---

### 核心功能开发（2025年12月）

#### 2025-12-30 ~ 2025-12-31：关键 Bug 修复

**重要修复**：
- **Huffman 编码边界问题**：处理单个重复字节码的 Huffman 树构建问题
- 文档更新

**问题描述**：当数据块只包含一种字符时，最小堆只剩一个节点，无法正确合并。

**解决方案**：
```cpp
// 检测单字符情况
if (countLeaves(treeroot) == 1) {
    // 根节点本身就是叶子
    // 编码直接设为 "0"
    hashtab[onlyChar].code = {0};
    hashtab[onlyChar].codelen = 1;
}
```

---

#### 2025-12-21 ~ 2025-12-25：文档完善

**开发内容**：
- 更新 README 文档
- 添加项目说明文档
- 优化 shell 交互体验

---

#### 2025-12-18 ~ 2025-12-19：代码规范化

**优化内容**：
- `std::map` 替换为 `std::unordered_map` 提升性能
- 优化命令行交互界面
- 为类及其成员添加详细注释和使用须知
- 清理临时文件（`.o`、`.bat`、`.ps1`）
- 添加 LICENSE 和 README

---

#### 2025-12-15 ~ 2025-12-17：解压模块迭代

**开发内容**：
- **解压模块 v0.0.3 ~ v0.0.5**
  - 实现目录恢复功能
  - 修复压缩与解压过程中的 Bug
  - 完成压缩功能主体开发
  - 编码转换问题修复（Windows 宽字符处理）

**关键实现**：
```cpp
void DecompressionLoop::decompressionLoop(Aes &aes)
{
    // 1. 读取目录块，填充文件队列
    // 2. 创建目录结构
    // 3. 处理每个文件：
    //    - 读取分割标准
    //    - 读取加密的 Huffman 树 → AES 解密 → 恢复树
    //    - 读取加密数据 → AES 解密 → Huffman 解码
    //    - 写入解压数据
}
```

---

#### 2025-12-13 ~ 2025-12-14：解压功能开发

**开发内容**：
- 准备解压流程
- 完成目录加密功能
- 开始解压功能调试

**解压流程设计**：
```
读取 .sy 文件
    → 验证魔数
    → 定位目录块
    → AES 解密
    → 解析目录结构
    → 创建目录队列、文件队列
    → 逐块读取、解密、解压、写入
```

---

#### 2025-12-10 ~ 2025-12-12：HeaderIterator 迭代器开发

**开发内容**：
- **目录遍历迭代器**（试作型 HeaderIterator 系列）
  - 经过多次迭代：MKII、MKIII、MK114514 等
  - 完成 BFS 广度优先搜索遍历目录树
  - 实现文件队列、目录队列管理

**BFS 算法实现**：
```cpp
void EntryProcessor::flowScanner(...)
{
    // 1. 写入逻辑根节点
    // 2. 处理用户指定的文件/目录列表
    // 3. 循环从队列取出目录，处理其内容
    // 4. 遇到子目录时加入队列继续遍历
}
```

**Huffman 模块集成**：
- 完成全功能 Huffman 压缩模块
- 实现压缩侧文件流程调试
- 加密模块组装完成

---

#### 2025-12-08 ~ 2025-12-09：AES 加密模块完成

**开发内容**：
- **AES-128 完整实现**
  - 密钥派生：SHA-256 哈希用户密码，取前 16 字节
  - 密钥扩展：生成 44 个轮密钥字
  - 10 轮加密：SubBytes → ShiftRows → MixColumns → AddRoundKey
  - 预计算表优化：`GF_MUL_2`、`GF_MUL_3` 等 GF(2^8) 乘法表

**性能优化**：
```cpp
// 预计算 GF(2^8) 乘法表，避免运行时计算
const uint8_t GF_MUL_2[256] = { /* 2*x mod ... */ };
const uint8_t GF_MUL_3[256] = { /* 3*x mod ... */ };
const uint8_t GF_MUL_9[256] = { /* 9*x mod ... */ };  // 解密用
const uint8_t GF_MUL_11[256] = { /* 11*x mod ... */ }; // 解密用
const uint8_t GF_MUL_13[256] = { /* 13*x mod ... */ }; // 解密用
const uint8_t GF_MUL_14[256] = { /* 14*x mod ... */ }; // 解密用
```

---

#### 2025-12-03 ~ 2025-12-06：HeaderWriter 功能扩展

**开发内容**：
- **HeaderWriter v0.1.5**
  - 添加符号链接（Symbol Link）支持
  - 增加 IV 头用于 AES 加密

**符号链接存储格式**：
```
| 标志 '4' | 链接名长度(4B) | 目标路径长度(4B) | 链接名 | 目标路径 |
```

**压缩模块调试**：
- 修复核心压缩逻辑
- 实现任务队列机制
- 更新数据传输接口

---

#### 2025-12-01 ~ 2025-12-02：数据传输接口

**开发内容**：
- 完成字节流数据块列表类 (`DataBlock = std::vector<uint8_t>`)
- 更新命名空间 `Y_flib` 与传输接口
- 实现数据块的管理与传输机制

**HeaderLoader 持续迭代**：
- v0.1.0：修复多个已知 Bug
- v0.1.1：优化读取逻辑，支持分块读取

---

### 基础架构搭建（2025年11月）

#### 2025-11-01 ~ 2025-11-13：HeaderReader 原型迭代与路径兼容性打底

**开发内容**：
- `HeaderReader` 从 Day6 原型持续迭代到 v0.1.5，逐步补齐位流读取、`filesystem` 接入、读取流程修正与一批早期 bug fix。
- 这段时间的提交基本围绕 `HeaderReader.h/.cpp` 高频改动展开，说明项目开始从“单独算法实验”转向“可读取文件头与目录信息的归档程序”。
- 引入 `MyQueue` 等辅助结构，为后续文件队列、目录队列和目录块解析打前站。
- 首次尝试使用宽字符方案处理 Windows 路径问题，为后来的中文路径兼容修复埋下基础。

**从提交中可确认的节点**：
- `Day6` / `Day6-bitsIO`：开始把读取逻辑从普通文件流操作往位级与结构化读取推进。
- `Reader` / `ReaderUpdate` / `Reader_filesystem`：补上基于文件系统路径的读取与调试。
- `HeaderReader-v0.1.1` ~ `v0.1.5`：持续修正接口、补充辅助队列、引入宽字符路径处理。

---

#### 2025-11-17 ~ 2025-11-21：HeaderLoader、DirectoryReader 与压缩主链路成型

**开发内容**：
- 引入 `HeaderLoader`、`FileLibrary` 等核心文件，开始统一文件头字段、目录读取入口与归档元数据定义。
- `HeaderReader` 在这一阶段逐步按职责更名并演化为 `DirectoryReader`，项目重心也从“读头”转向“读目录结构并驱动后续处理”。
- 线程池补上线程封装、任务列表、析构 `join`、命名线程销毁等基础能力，开始具备可持续运行的并发框架雏形。
- Huffman 频率统计模块基本完成，并开始接入 `encode` 压缩模块，核心函数签名同步调整。
- 同期也做了不少工程卫生工作，包括注释乱码修复、无用语句清理、任务对象放堆上管理等。

**从提交中可确认的节点**：
- `HeaderLoader` 系列提交：开始拆出专门的头部/目录加载器。
- `HeaderReader-v0.1.6 -- Logical Header`：引入逻辑头概念，向“逻辑根目录”方案靠拢。
- `DirctoryReader-v0.1.6 -- Rename`：名称正式从 `HeaderReader` 转向 `DirectoryReader`。
- `频率统计模块基本完成。封装线程池，编写多线程处理`、`encode压缩模块`：压缩主链路首次成型。

---

#### 2025-11-29 ~ 2025-11-30：HeaderLoader 与 AES-CFB

**开发内容**：
- **HeaderLoader v0.0.1 ~ v0.0.5**
  - 实现文件头读取功能
  - 完成 AES-CFB 模式适配（支持流式加密）
  - 添加 IV（初始化向量）支持
  - 实现目录块的加载与解析

**AES 模式选择**：
- 初期使用 ECB 模式（简化实现）
- 后改为 CFB 模式支持流式加密
- 每个 16KB 目录块使用独立的 IV

**关键实现**：
```cpp
struct Header {
    SizeOfMagicNum magicNum_1 = 0;           // 0xDEADBEEF
    CompressStrategy strategy = 0;           // 压缩策略
    CompressorVersion version = 0;           // 版本号
    HeaderOffsetSize headerOffset = 0;       // 头部偏移
    DirectoryOffsetSize directoryOffset = 0; // 目录偏移
    SizeOfMagicNum magicNum_2 = 0;           // 0xDEADBEEF
};  // 共 11 字节
```

---

#### 2025-11-27 ~ 2025-11-28：HeaderWriter 调试

**修复问题**：
1. `sizeof` 返回值类型问题：在 32 位环境下 `sizeof` 返回 `size_t`，与 `uint64_t` 运算时需注意类型转换
2. 偏移量计算问题：多次修复 `Offset` 相关的边界情况
3. 文件指针定位问题：确保 `seekg/seekp` 正确工作

---

#### 2025-11-25 ~ 2025-11-26：编码树序列化

**开发内容**：
- **HeaderWriter v0.1.4**
  - 完成编码树的保存与加载模块
  - 实现序列化 I/O 功能
  - 修复 Offset 相关问题（`sizeof` 返回值类型问题，需强制转换为 `uint64_t`）

**关键代码**：
```cpp
// 文件标准的基础大小（不含变长文件名）
constexpr ConstSize FILE_STANDARD_SIZE_BASIC =
    FLAG_SIZE +                    // 1字节标志
    sizeof(FileNameSize) +         // 4字节文件名长度
    sizeof(FileSize) * 2;          // 8+8字节原始大小和压缩大小
```

**清理**：删除 `HefftabBlock` 模块，优化代码结构。

---

#### 2025-11-23 ~ 2025-11-24：HeaderWriter 与 Huffman 编码表

**开发内容**：
- **HeaderWriter v0.1.2 ~ v0.1.3**
  - 完成文件头写入的基础框架
  - 实现编码表数据类 `EntryDetails`
  - 开发生成编码表并向外传递的功能模块 (`GenHeffcodeTab`)
  - 开发从外接受编码表并生成解码树的功能模块 (`LoadHeffcodeTab`)

**技术实现**：
```cpp
// 编码表序列化格式
// 'l' + char 表示叶子节点
// 'r' 表示内部节点
void tree_to_plat_uchar(sfc::block_t& out_block);
```

**优化**：将 `std::map` 替换为 `std::unordered_map`（后改为自定义 `Heffmap`）以提升哈希查找性能。

---

### 项目初始化与框架设计（2025年9月-10月）

#### 2025-10-28 ~ 2025-10-30：任务调度优化 Day5

**开发内容**：
- 优化线程池任务调度
- 实现任务队列管程
- 添加 `Locator` 定位器类（文件指针定位工具）
- 将原先分散的任务队列列表与条件变量列表收口成更明确的管程式封装，`get_task()` 语义也在这一阶段稳定下来。
- `Locator` 开始承担从文件头、目录块和数据区之间来回跳转的职责，后续偏移量回填、目录块定位都依赖这一层基础工具。

**任务队列管程**：
```cpp
class TaskMonitor {
    std::vector<TaskQueue> queues;  // 任务队列列表
    std::condition_variable cv;      // 条件变量
    std::mutex mtx;                  // 互斥锁
public:
    void add_task(int queue_id, Task task);
    Task get_task(int queue_id);
};
```

**Locator 定位器**：
```cpp
class Locator {
public:
    void locateFromBegin(std::ofstream& file, uint64_t offset);
    void locateFromBegin(std::ifstream& file, uint64_t offset);
    void locateFromEnd(std::ofstream& file, uint64_t offset);
};
```

---

#### 2025-10-26：流程图设计 Day3-4

**开发内容**：
- 绘制压缩流程图
- 设计解压流程
- 规划数据流向

**压缩流程**：
```
输入文件 → 读取数据块 → Huffman 编码 → AES 加密 → 写入输出
    ↑                                              ↓
    └────────────── 线程池调度 ──────────────────────┘
```

---

#### 2025-10-23 ~ 2025-10-24：线程池与任务队列 Day2

**开发内容**：
- **线程池模块** (`ThreadPool`)
  - 创建工作线程管理
  - 实现任务分发机制
- **任务队列模块** (`TaskQueue`)
  - 封装条件变量 (`std::condition_variable`)
  - 实现线程安全的任务入队/出队操作
- **策略模式接口**
  - `CompressionStrategy`：压缩策略接口
  - `EncryptionStrategy`：加密策略接口
  - `DataInteraction`：数据交互接口
- 补上 `IModule` 抽象、命名空间与数据块类型定义，开始为“模块串联”而不是“单文件函数调用”做准备。
- `blocks_t` / `block_t` 的表达在这两天多次调整，说明当时正在摸索数据块究竟该按值传、按引用传，还是以指针列表组织。

**关键代码**：
```cpp
// 功能模块接口（抽象类）
class IModule {
public:
    virtual void work(block_t* data) = 0;
    virtual ~IModule() = default;
};

// 任务队列管程
class TaskQueue {
    std::queue<Task> tasks;
    std::mutex mtx;
    std::condition_variable cv;
public:
    void push(Task task);
    Task get_task();
};
```

---

#### 2025-10-21：框架搭建 Day0

**开发内容**：
- 重构文件结构，分离头文件和源文件
- 重命名 `FileStream` 为 `DataStream`
- 开始把项目从最早的 `src/`、`include/` 散装结构，迁移为 `CompressionModules`、`EncryptionModules`、`DataStream` 这类按职责拆分的目录。
- 同期发生了多次目录重命名和清理，例如 `CompresssionModules` → `CompressionModules`，为后续大规模迭代先把骨架搭起来。

**文件结构**：
```
CompressionModules/
├── heffman/           # Huffman 压缩模块
│   ├── include/
│   └── src/
├── hefftype/          # Huffman 数据结构
└── EncryptionModules/
    └── aes/           # AES 加密模块

CompressorFileSystem/
└── DataCommunication/  # 数据交互模块
```

---

#### 2025-10-11 ~ 2025-10-18：架构设计与规划

**开发内容**：
- 完善项目策划文档
- 设计 UML 类图和流程图
- 规划模块间的交互接口
- 确定技术栈和编码规范
- 这一阶段的提交几乎都集中在 `plan.md` / 策划文档，说明主要工作是先把“压缩、加密、调度、数据交互”几层关系想清楚，再进入实现。
- 后续出现的 `Scheduler`、`ThreadPool`、`TaskQueue`、`DataInteraction` 等概念，基本都能在这段策划期找到雏形。

**架构设计**：
```
┌─────────────────────────────────────────────────────────┐
│                    Scheduler (调度器)                    │
├─────────────────────────────────────────────────────────┤
│    CompressionStrategy    │    EncryptionStrategy       │
│    (压缩策略接口)           │    (加密策略接口)            │
├─────────────────────────────────────────────────────────┤
│              DataInteraction (数据交互接口)               │
│              ┌─────────────────────────────┐            │
│              │  ThreadPool (线程池)         │            │
│              │  TaskQueue (任务队列)        │            │
│              └─────────────────────────────┘            │
└─────────────────────────────────────────────────────────┘
```

---

#### 2025-10-01 ~ 2025-10-05：AES 加密模块开发

**开发内容**：
- **AES-128 加密模块 v1.0**
  - 实现完整的 AES-128 加密算法
  - 支持 ECB 模式
  - 完成密钥扩展（Key Expansion）
  - 实现 SubBytes、ShiftRows、MixColumns、AddRoundKey 四个核心变换
- 代码层面先后落到了 `include/Aes.h`、`src/AesFunctions.cpp`、`src/mainCircuit.cpp` / `mainCircle.cpp` 等文件中，属于典型的“先做算法可运行样例，再逐步整理结构”的阶段。
- 这几天也伴随了较多工程清理：反复修正 `.gitignore`，移除 `bin/`、`.vscode/` 和临时主程序文件，避免把实验产物继续带进仓库。

**技术实现**：
```cpp
// AES 核心变换
void SubBytes(state_t &state);      // S盒替换
void ShiftRows(state_t &state);     // 行移位
void MixColumns(state_t &state);    // 列混合
void AddRoundKey(state_t &state);   // 轮密钥加
```

**优化**：
- 预计算 GF(2^8) 乘法表，避免运行时计算
- 使用查表法加速 S 盒和逆 S 盒操作

---

#### 2025-09-29 ~ 2025-09-30：项目初始化

**开发内容**：
- 项目初始化，创建基础文件结构
- 编写策划文档（策划.md）
- 配置 `.gitignore` 忽略规则
- 初始化 Git 仓库，创建 develop 和 master 分支
- 最早一批提交已经包含 `src/Aes.cpp`、`src/Aes.h`、`bin/Aes.exe` 一类 AES 试验文件，说明项目起点更接近“加密算法实验场”而不是完整归档工具。
- 9 月 30 日的多次提交主要在做仓库清理、冲突解决和策划补写，把误提交文件与重复目录一点点收回到可继续开发的状态。

**项目结构**：
```
We_compress/
├── src/
├── include/
├── plans/
│   └── plan.md
└── .gitignore
```

---
